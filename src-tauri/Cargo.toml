[package]
name = "ytsage-rust"
version = "0.1.0"
edition = "2024"

[package.metadata]
app = "ytsage-rust"

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tauri = { version = "2.0", features = [] }
thiserror = "2.0"
uuid = { version = "1.18", features = ["v4", "serde"] }

[lib]
name = "ytsage_rust_lib"
crate-type = ["staticlib", "cdylib", "rlib"]
